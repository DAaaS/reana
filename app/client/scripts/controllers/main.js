'use strict';

/**
 * @ngdoc function
 * @name reanaApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the reanaApp
 */
angular.module('reanaApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
